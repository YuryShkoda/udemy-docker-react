language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t yuryshkoda/udemy-docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true yuryshkoda/udemy-docker-react npm run test